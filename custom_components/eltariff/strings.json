{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Grid Tariff API",
        "data": {
          "base_url": "API base URL",
          "bearer_token": "Bearer token (optional)"
        }
      },
      "tariff": {
        "title": "Select tariff",
        "data": {
          "tariff_id": "Tariff",
          "vat_mode": "VAT mode"
        }
      },
      "options": {
        "title": "Options",
        "data": {
          "vat_mode": "VAT mode",
          "bearer_token": "Bearer token (optional)",
          "power_sensor_entity_id": "Power meter sensor (optional)",
          "power_sensor_unit": "Power unit"
        },
        "data_description": {
          "power_sensor_entity_id": "A sensor reporting real-time power consumption. Used to calculate actual peak and billing-period cost.",
          "power_sensor_unit": "Unit reported by the selected sensor."
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the API",
      "auth_error": "Authentication failed — check bearer token",
      "unknown": "Unexpected error"
    },
    "abort": {
      "already_configured": "This tariff is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Options",
        "data": {
          "vat_mode": "VAT mode",
          "bearer_token": "Bearer token (optional)",
          "power_sensor_entity_id": "Power meter sensor (optional)",
          "power_sensor_unit": "Power unit"
        },
        "data_description": {
          "power_sensor_entity_id": "A sensor reporting real-time power consumption. Used to calculate actual peak and billing-period cost.",
          "power_sensor_unit": "Unit reported by the selected sensor."
        }
      }
    }
  }
}
